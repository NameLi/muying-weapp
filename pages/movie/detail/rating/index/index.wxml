<half-page round height="80vh" title="评分分布（示例）" bind:close="closePage">
    <view class="loading" wx:if="{{loading}}">
        <m-spinner type="triple" color="#f60, #7fd901, #36a2e0" />
    </view>

    <view class="tip">评分分布功能因样本原因还在开发中，以下仅为示例数据</view>

    <!-- 详情 -->
    <rate-detail rating="{{rating}}" vote-count="{{voteCount}}" ratios="{{ratios}}" better-than="{{betterThan}}" />

    <!-- 趋势 -->
    <!-- <rate-trend :months="months" /> -->
</half-page>