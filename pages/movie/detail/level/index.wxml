<view class="list">
  <view class="item m-hairline--bottom" wx:for="{{list}}" wx:key="id">
    <text class="content">{{ item.content }}</text>
  </view>
</view>

<m-loadmore wx:if="{{loading}}" loading="{{loading}}" />